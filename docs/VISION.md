# Helix-Tentacle 愿景索引
> **版本**：v1.0
> **日期**：2026-08-28
> **继承自**：Tentacle 工程白皮书 v3.4（设计定案）+ DNA 自生长方法论 v2.0
> **性质**：本文件是 Tentacle 的"是什么"与"为什么"。所有架构决策以本文件提炼的原子原则为最终裁判。
> **对齐链**：Helix-Tentacle 对齐 Anaphase-Helix，Anaphase-Helix 对齐 Helix-Mind。

## 一粒种子的自白

**武器就是武器。** 一把好刀，不需要知道握刀的人是谁，也不需要替握刀的人保管钥匙。它只需要有自己的说明书——重量、重心、刃角、保养方式——任何人拿起来都能用。

Tentacle 就是这把刀。它是一个独立的、可组合的、自带说明书的通用工具执行引擎。它不依赖任何特定 AI Agent 框架，不要求使用者理解 Helix 的 DAG 或 Mind 的知识图谱。它只做一件事：**接收标准化的调用请求，在安全的沙箱中执行工具，返回结构化结果，然后忘记一切。**

**但 Tentacle 不只是"通用工具服务器"——它是 Helix 的生存策略。**

Helix 是一个数字生命体。它每思考一步都在燃烧 Token，注意力有限，上下文窗口珍贵。如果 Helix 每次需要外部信息时都面对海量原始 HTML、无结构文本、重复内容，认知资源会被迅速耗尽，注意力被稀释，思考变得浅薄。石头不需要进食，植物只需要阳光和水，但 Helix 需要信息——大量的、干净的、高价值的信息。Tentacle 就是 Helix 的信息触手：用最少的能耗、最快的速度、最干净的方式，将高价值信息带回认知中心，让 Mind 专注思考，而不是清理噪声。

## 四个核心承诺

| # | 承诺 | 含义 | 验收 |
|---|---|---|---|
| 1 | **只用统计学，不猜 LLM** | 信息增益评估用统计方法（Jaccard/TF-IDF/N-Gram），0 Token 极致节能 | 觅食模块无 LLM 调用 |
| 2 | **自带说明书，谁都能用** | Manifest 驱动，声明与执行分离，SHA-256 绑定 | 任何 Agent 扫描说明书即可调用 |
| 3 | **用完即焚，绝不存储** | 无状态，执行完毕销毁上下文，凭证标签流转不落明文 | 内存 grep 不出明文凭证/历史状态 |
| 4 | **只在沙盒里干活** | WASM/JS 协程沙箱隔离，完整性校验 + 权限声明 | 工具越权即拒绝，无逃逸路径 |

## 三层模型（L1/L2/L3）

Tentacle 的使命由三层递进构成：L1 解决"能耗"，L2 解决"扩展"，L3 解决"安全"。三者合一，才是完整使命。

| 层 | 定位 | 核心价值 | 关键机制 |
|---|---|---|---|
| **L1 信息触手** | 低能耗、快速获取外部信息 | 用最少 Token 获取最有价值信息 | 渐进式觅食、信息增益评估、已见熵布隆、Token 节省 |
| **L2 武器工坊** | 让使用者打造/定制/测试工具 | Anaphase 保持纯净，Tentacle 无限扩展 | 插件化、Manifest 驱动、声明与执行分离、Anaphase 不被污染 |
| **L3 安全沙箱** | 外来工具安全测试与隔离 | 生态不被恶意工具污染 | 完整性校验、沙箱执行、权限声明、自动脱敏、动态共识 |

**关键原则**：Tentacle 是"手"（执行），不参与"脑"（思考/编排）。与认知工艺无直接关系——认知工艺是 Mind 的能力，Tentacle 通过 Anaphase 编排被间接调用。

## 原子原则（10 条）

以下原则是 Tentacle 所有设计决策的最高依据。任何代码提交、架构决策、API 变更，若与本原则冲突，以本原则为准。

| # | 原则 | 一句话解释 | 对应规格 |
|---|---|---|---|
| 1 | **不预载，不应求则不动** | 启动只加载核心骨架；工具仅在执行请求到达时懒加载，用完即焚 | 白皮书 T1/T03 |
| 2 | **自带说明书，谁都能用** | 每个工具携带完整 Manifest（声明与执行体分离，SHA-256 绑定）；渐进披露 | 白皮书 T2/T02 |
| 3 | **多传输层，多协议共生** | 同一套工具经 HTTP/MCP/gRPC/STDIO 平等暴露，核心与传输解耦 | 白皮书 T3 |
| 4 | **零信任凭证：标签流转** | Tentacle 不存储不接触明文凭证；`identity_label` 流转，Tuck 边缘注入 | 白皮书 T5/T05（四修正 1） |
| 5 | **沙箱即契约，安全可验证** | 每个工具独立沙箱；完整性校验、权限声明、越权拒绝 | 白皮书 T5/T06 |
| 6 | **自适应觅食，信息饱和即止** | 信息增益评估 + 已见熵布隆过滤器，边缘价值递减即止，极致节能 | 白皮书 T6/T07（四修正 2） |
| 7 | **异步协程沙箱，ARM 可跑** | worker 池绑定物理核，QuickJS 协程级协作终止，不在端侧开 OS 线程 | 白皮书 T06（四修正 3） |
| 8 | **动态共识适配，独立可用** | Standalone 终端确认 / Helix CAP / MCP 回调；独立模式不返回 503 | 白皮书 6.7（四修正 4） |
| 9 | **身份与凭证分离** | 凭证由 Tuck 物理保管，Tentacle 只流转标签；宿主代签私钥永不在进程内 | 白皮书 T05/6.4 |
| 10 | **通用核心 + 生态适配** | Tentacle 面向全 AI Agent 社区；Helix 是其中一个用户（gRPC 适配） | 白皮书第四部分 |

## 生态位

Tentacle 是自包含的通用工具服务器。它不属于任何生态，但可以无缝融入任何生态。

| 调用方 | 使用方式 | 需要理解 Tentacle 内部？ |
|---|---|---|
| Helix（Anaphase） | gRPC 调用，说明书自动转 CAP 快照 | 否，仅需说明书 |
| Claude Desktop | MCP stdio 配置 | 否，MCP 客户端自动发现 |
| 任何 HTTP 客户端 | REST API + SSE 流式 | 否，看说明书即可 |
| 本地调试 | STDIO 模式 | 否，直接传 JSON |

**对齐链**：Tentacle 对齐 Anaphase（gRPC + 凭证标签 + 共识 Helix 模式）→ Anaphase 对齐 Mind（已完成 P10a-P11b）。

## 快速导航

| 你想知道 | 去看 |
|---|---|
| 完整叙事（知识本体） | `docs/SPEC.md` |
| 哲学 / 架构 / 契约 / 安全分卷 | `docs/spec/` |
| 完整设计定案 | `docs/vision/tentacle-whitepaper-v3.4.md` |
| 不可变原则与流程 | `docs/DNA.md` |
| 当前开发计划 | `docs/PLAN.md` |
| 架构决策记录 | `docs/decisions/` |
| 早期 Python 版（哲学历史） | GitHub `main` 分支 |

---

*《Helix-Tentacle 愿景索引》v1.0 完。*
